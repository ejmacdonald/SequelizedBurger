<li class="list-group-item">
  {{burger_name}}
  <div class="float-right">
    <span class="input-group-btn">
      <form>
        <input type="hidden" name="devoured" value="true">
        {{#unless devoured}}
        	<button class="btn btn-success btn-lg eat-btn" data-id="{{id}}" type="submit"> EAT IT! </button>
        {{/unless}}
      </form>
    </span>
  </div>
</li>

